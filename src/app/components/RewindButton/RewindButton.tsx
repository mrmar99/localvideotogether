import React from 'react';

import { Button } from "antd";
import ReactPlayer from 'react-player';

type RewindButtonProps = {
  video: React.RefObject<ReactPlayer>;
};

export const RewindButton: React.FC<RewindButtonProps> = () => {
  return <></>;
};